export interface Card {
  suit: string;        // Nom français (ex: "Trèfle")
  rank: string;        // Nom français (ex: "As")
  displayName: string; // "As de Pique"
  imageName: string;   // "ace_of_spades.png"
}